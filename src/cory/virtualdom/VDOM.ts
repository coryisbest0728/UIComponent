/**
 * @file The virtual dom node interface.
 *     This interface has extended from VNode in the maquettejs.
 * @see http://maquettejs.org/
 * @see http://maquettejs.org/docs/typedoc/interfaces/_maquette_.vnode.html
 *
 * @author Cory(https://github.com/coryisbest0728)
 */

import { VNode } from 'maquette';

export interface VDOM extends VNode {}